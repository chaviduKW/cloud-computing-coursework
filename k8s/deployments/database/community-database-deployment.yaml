apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresCommunityDb
  namespace: techsalary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgresCommunityDb
  template:
    metadata:
      labels:
        app: postgresCommunityDb
    spec:
      containers:
        - name: postgresCommunityDb
          image: postgres:16
          ports:
            - containerPort: 5435
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_COMMUNITY_DB

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: postgres-db
              mountPath: /var/lib/postgresql/data
            - name: init-script
              mountPath: /docker-entrypoint-initdb.d/
      volumes:
        - name: postgres-db
          persistentVolumeClaim:
            claimName: postgres-storage-community
        - name: init-script
          configMap:
            name: community-init-script