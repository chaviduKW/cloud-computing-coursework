apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgrescommunitydb
  namespace: techsalary-data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgrescommunitydb
  template:
    metadata:
      labels:
        app: postgrescommunitydb
    spec:
      containers:
        - name: postgrescommunitydb
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_COMMUNITY_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: postgrescommunitydb-data
              mountPath: /var/lib/postgresql/data
            - name: init-script
              mountPath: /docker-entrypoint-initdb.d/
      volumes:
        - name: postgrescommunitydb-data
          persistentVolumeClaim:
            claimName: postgrescommunitydb-storage
        - name: init-script
          configMap:
            name: postgres-init-community-script
