kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-storage-community
  namespace: techsalary-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi