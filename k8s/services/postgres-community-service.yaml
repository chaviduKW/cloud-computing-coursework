apiVersion: v1
kind: Service
metadata:
  name: postgres-community
  namespace: techsalary-data
spec:
  selector:
    app: postgres-community
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
