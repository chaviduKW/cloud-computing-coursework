apiVersion: v1
kind: Service
metadata:
  name: postgrescommunitydb
  namespace: techsalary-data
spec:
  selector:
    app: postgrescommunitydb
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
