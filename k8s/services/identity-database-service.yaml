apiVersion: v1
kind: Service
metadata:
  name: postgresidentitydb
  namespace: techsalary-data
spec:
  selector:
    app: postgresidentitydb
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
