apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: techsalary
  labels:
    app: frontend
    version: v1
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  sessionAffinity: None
